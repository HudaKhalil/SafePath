# Backend Environment Variables
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Option 1: Individual connection params
DB_HOST=localhost
DB_PORT=5432
DB_NAME=london_safety_routing
DB_USER=postgres
DB_PASSWORD=ilove_Theo12345

# Option 2: Full database URL (alternative to individual params)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/london_safety_routing

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_minimum_32_chars
BCRYPT_SALT_ROUNDS=10

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000